# Dependencias del sistema de análisis de XAU/USD (paquete `oro`).
# Instalación:  pip install -r oro/requirements.txt
#
# El núcleo (dominio, indicadores, estructura, riesgo, features, señales,
# backtesting) solo necesita numpy y pandas. El resto son opcionales según el
# uso: ML (scikit-learn), API/panel (fastapi/uvicorn), notificaciones (requests).

numpy>=1.24            # Cálculo numérico (indicadores, features, métricas).
pandas>=2.0            # Series temporales OHLCV y manipulación de datos.

# --- Opcional: modelo de probabilidad y validación walk-forward ---
scikit-learn>=1.3      # Gradient Boosting (HistGradientBoostingClassifier).

# --- Opcional: API HTTP y panel de control ---
fastapi>=0.110         # API de señales, backtest y salud.
uvicorn[standard]>=0.27  # Servidor ASGI para la API/panel.

# --- Datos reales y sentimiento (modo «vivo») + notificaciones HTTP ---
# Necesario para: precio real (Yahoo), prensa/sentimiento y calendario macro,
# y para enviar alertas por Telegram/webhook. El RSS se parsea con la stdlib.
requests>=2.31
